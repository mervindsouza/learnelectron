#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../elementary-electron/bin/elementary-electron" "$@"
  ret=$?
else 
  node  "$basedir/../elementary-electron/bin/elementary-electron" "$@"
  ret=$?
fi
exit $ret
